# Reference only. Put real values in the Gateway process environment,
# a secret manager, or ~/.openclaw/.env — never commit them here.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
OPENAI_API_KEY=
VOICE_CALL_PUBLIC_URL=
